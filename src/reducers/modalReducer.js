
let initialState = {
    changePasswordModal: false,
    viewProfileModal: false
}

const modelReducer = (state=initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export { modelReducer }